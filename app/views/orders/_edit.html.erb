<%if @order.new_record?
    url = orders_path
  else
    url = order_path(@order.id)
  end %>
<% params[:order].nil? ?
    team = @order.get_team.id :
    team = params[:order][:team]
 %>
<%= labelled_form_for @order, url: url, :html => {:id => 'order-form', :multipart => true} do |f| %>
    <%= error_messages_for @order if @order.present?%>
    <div class="box">
      <fieldset class="tabular"><legend><%= l(:label_change_properties) %></legend>
      <div id="all_attributes">
        <%= labelled_fields_for :order, @order do |f| %>
          <p><%= f.text_field :name, :size => 80, :maxlength => 255, :required => true %></p>

          <p>
            <%= f.label_for_field :description %>
            <%= f.text_area :description,
                            :cols => 60,
                            :rows => 10,
                            :accesskey => accesskey(:edit),
                            :class => 'wiki-edit',
                            :no_label => true %>
          </p>
          <p><%= f.select :team, options_for_select(@groups.collect {|t| [t.name, t.id]}, selected: team), {:required => true} %></p>
          <p><%= f.number_field :invoiced_budget, :class => 'invoiced_budget', :step => 0.5, :required => true %></p>
          <p><%= f.number_field :allocatable_budget, :class => 'allocatable_budget', :step => 0.5, :required => true %></p>
        <% end %>
      </div>
      </fieldset>
    </div>
    <%= submit_tag l(:button_submit) %>
    | <%= link_to l(:button_cancel), {}, :onclick => "jQuery.noConflict()('#update').hide(); return false;" %>
<% end %>
