<div class="contextual">
<%= link_to l(:label_role_new), new_tocat_role_path, :class => 'icon icon-add' %>
</div>

<h2><%=l(:label_role_plural)%></h2>

<table class="list">
  <thead><tr>
    <th><%=l(:label_role)%></th>
  <th></th>
  </tr></thead>
  <tbody>
<% for role in @roles %>
  <tr class="<%= cycle("odd", "even") %>">
  <td><%= content_tag('span', link_to(h(role.name), edit_tocat_role_path(role))) %></td>
  <td class="buttons">
    <%= delete_link tocat_role_path(role)%>
  </td>
  </tr>
<% end %>
  </tbody>
</table>



<% html_title(l(:label_role_plural)) -%>
